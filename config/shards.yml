octopus:
  environments:
    - development
    - production
  replicated: true
  fully_replicated: true
  development:
    slave1:
      adapter: mysql2
      host: localhost
      database: mint2
      username: root
    slave2:
      adapter: mysql2
      host: localhost
      database: mint
      username: root
  production:
    slave2:
      adapter: mysql2
      database: rails_app